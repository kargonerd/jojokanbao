# Monorepo-local configuration template.
# Copy required entries to .env.local and never commit real secrets.

# Supabase Management API (temporary local administration only)
SUPABASE_ACCESS_TOKEN=
SUPABASE_PROJECT_REF=

# Shared browser auth configuration (public values embedded by Vite)
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Press Engine / MinerU
MINERU_API_BASE=
MINERU_API_TOKEN=
