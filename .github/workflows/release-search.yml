name: Release · Reader Search

on:
  push:
    tags:
      - "search-v*"

permissions:
  contents: read

concurrency:
  group: reader-search-production
  cancel-in-progress: false

jobs:
  verify-release:
    name: Verify release tag
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5
        with:
          fetch-depth: 0

      - name: Require a tagged master commit
        shell: bash
        run: |
          tag_ref="refs/tags/$GITHUB_REF_NAME"
          # actions/checkout may recreate the event ref as a lightweight tag
          # pointing at GITHUB_SHA. Fetch the remote tag object explicitly so
          # the annotated-tag requirement checks what was actually pushed.
          git fetch --force origin \
            "refs/heads/master:refs/remotes/origin/master" \
            "$tag_ref:$tag_ref"
          test "$(git cat-file -t "$tag_ref")" = "tag" || {
            echo "Search releases require an annotated tag." >&2
            exit 1
          }
          tagged_commit="$(git rev-list -n 1 "$tag_ref")"
          test "$tagged_commit" = "$GITHUB_SHA" || {
            echo "Search release event SHA does not match the annotated tag." >&2
            exit 1
          }
          git merge-base --is-ancestor "$tagged_commit" origin/master || {
            echo "Search releases must tag a commit already merged into master." >&2
            exit 1
          }

  deploy-staging:
    name: Deploy staging
    needs: verify-release
    runs-on: ubuntu-latest
    environment: search-staging
    timeout-minutes: 15
    env:
      PYTHONUNBUFFERED: "1"
      TENCENTCLOUD_SECRET_ID: ${{ secrets.COS_SECRET_ID }}
      TENCENTCLOUD_SECRET_KEY: ${{ secrets.COS_SECRET_KEY }}
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5

      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: "3.9"
          cache: pip
          cache-dependency-path: infrastructure/tencent-scf/search/requirements-deploy.txt

      - name: Install deployment dependencies
        run: >-
          python -m pip install --disable-pip-version-check
          -r infrastructure/tencent-scf/search/requirements-deploy.txt

      - name: Verify credentials
        shell: bash
        run: |
          test -n "$TENCENTCLOUD_SECRET_ID" || {
            echo "COS_SECRET_ID is not configured." >&2
            exit 1
          }
          test -n "$TENCENTCLOUD_SECRET_KEY" || {
            echo "COS_SECRET_KEY is not configured." >&2
            exit 1
          }

      - name: Deploy and verify staging
        run: python infrastructure/tencent-scf/search/deploy.py --target staging

  deploy-production:
    name: Deploy production
    needs: deploy-staging
    runs-on: ubuntu-latest
    environment: search-production
    timeout-minutes: 15
    env:
      PYTHONUNBUFFERED: "1"
      TENCENTCLOUD_SECRET_ID: ${{ secrets.COS_SECRET_ID }}
      TENCENTCLOUD_SECRET_KEY: ${{ secrets.COS_SECRET_KEY }}
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5

      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: "3.9"
          cache: pip
          cache-dependency-path: infrastructure/tencent-scf/search/requirements-deploy.txt

      - name: Install deployment dependencies
        run: >-
          python -m pip install --disable-pip-version-check
          -r infrastructure/tencent-scf/search/requirements-deploy.txt

      - name: Verify credentials
        shell: bash
        run: |
          test -n "$TENCENTCLOUD_SECRET_ID" || {
            echo "COS_SECRET_ID is not configured." >&2
            exit 1
          }
          test -n "$TENCENTCLOUD_SECRET_KEY" || {
            echo "COS_SECRET_KEY is not configured." >&2
            exit 1
          }

      - name: Deploy and verify production
        run: >-
          python infrastructure/tencent-scf/search/deploy.py
          --target production
          --confirm-production
