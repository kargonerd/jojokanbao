# TODO

## P0

- 定义 `jojo-press -> jojo-rag` 导入包格式
- 设计导入接口
- 明确 notebook / source 与校对后书籍数据的映射规则

## P1

- 补齐文库发布校验
- 补齐 source 发布校验
- 补齐封面、描述、绑定文档、图片等完整性校验
- 让未完成配置的文库默认不可发布

## P2

- 收尾后台 UI 细节
- 统一后台编辑页与列表页样式
- 为当前 `/chat`、`/source`、`/admin` 主流程补更稳妥的验证方式

## P3

- 对接 `jojo-press` 导出的中间结果或导入包
- 让阅读器消费校对完成后的文档
- 让问答链路消费校对后的结构化文本
